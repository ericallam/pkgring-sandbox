---
"@pkgring/sdk": patch
---

Cap input name length at 50 chars in `welcome()`. Test 8 hotfix shipped from `release/0.1.x` while main is at 0.2.0 — used to verify whether `changesets/action` overwrites the `latest` dist-tag with an older version.
