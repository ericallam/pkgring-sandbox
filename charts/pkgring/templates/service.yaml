apiVersion: v1
kind: Service
metadata:
  name: {{ include "pkgring.fullname" . }}
  labels:
    {{- include "pkgring.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pkgring.selectorLabels" . | nindent 4 }}
