# @pkgring/core

## 0.3.0

### Minor Changes

- 2204104: Add `shout()` function — emits a screaming greeting. Stand-in for "merged but unreleased PR queued on main" used to test Scenario B in TESTING.md.

## 0.2.0

### Minor Changes

- e570f91: Add `farewell()` function for symmetric goodbye messages. Drives the 0.2.0 release used to reproduce the npm `latest` clobber from release-branch hotfixes.

## 0.1.0

### Minor Changes

- 918a29d: First real release. Establishes the 0.1.0 baseline so the release pipeline test matrix (TESTING.md) has something to hotfix off of.
